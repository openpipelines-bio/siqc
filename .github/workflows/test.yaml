name: Test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dataset:
          - name: single-cell
            type: sc
            path: resources_test/sc_dataset
            output: single-cell-report.html
          - name: single-cell-large
            type: sc_large
            path: resources_test/sc_dataset_large
            output: single-cell-large-report.html
          - name: spatial
            type: xenium
            path: resources_test/xenium_dataset
            output: spatial-report.html
          - name: spatial-large
            type: xenium_large
            path: resources_test/xenium_dataset_large
            output: spatial-large-report.html
          - name: cosmx
            type: cosmx
            path: resources_test/cosmx_dataset
            output: cosmx-report.html
          - name: cosmx-large
            type: cosmx_large
            path: resources_test/cosmx_dataset_large
            output: cosmx-large-report.html
          - name: visium
            type: visium
            path: resources_test/visium_dataset
            output: visium-report.html
          - name: visium-large
            type: visium_large
            path: resources_test/visium_dataset_large
            output: visium-large-report.html
          - name: integration
            type: integration
            path: resources_test/integration_dataset
            output: integration-report.html
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Install CLI tool globally
        run: npm install -g .

      - name: Prepare output directory
        run: mkdir -p output

      - name: Generate test data for ${{ matrix.dataset.name }}
        run: siqc generate-test-data --type ${{ matrix.dataset.type }} --output ${{ matrix.dataset.path }}

      - name: Build ${{ matrix.dataset.name }} report using CLI
        run: |
          siqc render \
            --data ${{ matrix.dataset.path }}/data.json \
            --structure ${{ matrix.dataset.path }}/structure.json \
            --output output/${{ matrix.dataset.output }}

      - name: Upload ${{ matrix.dataset.name }} report
        uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.dataset.name }}-report
          path: output/${{ matrix.dataset.output }}
